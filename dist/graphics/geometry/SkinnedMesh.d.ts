import { StaticMesh } from "./StaticMesh";
import { Entity } from "../../core/Entity";
import { SerializableMatrix44 } from "../../math/Matrix44";
import { VertexBuffer } from "../VertexBuffer";
import { Shader } from "../Shader";
import { MemoryTexture } from "../MemoryTexture";
export declare class SkinnedMesh extends StaticMesh {
    static skeletonPropertyKey: string;
    set skeleton(entity: Entity | null);
    get skeleton(): Entity | null;
    get bindMatrix(): SerializableMatrix44;
    get bindMatrixInverse(): SerializableMatrix44;
    get boneTexture(): MemoryTexture;
    get boneTextureSize(): number;
    get boneMatrices(): Float32Array;
    set bindMatrix(bindMatrix: SerializableMatrix44);
    set boneFbxIds(ids: number[]);
    private _skeleton;
    private _bindMatrix;
    private _bindMatrixInverse;
    private _boneFbxIds;
    private _vb;
    private _boneTexture;
    private _boneTextureSize;
    private _boneMatrices;
    private _boneInverses;
    private _bones;
    constructor();
    getVertexBuffer(): VertexBuffer | null;
    destroy(): void;
    updateMatrices(): void;
    updateShader(shader: Shader, bucketId: string): void;
    private initMatrices;
}
